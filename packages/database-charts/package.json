{
  "name": "@potato/database-charts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:migrate:create": "prisma migrate dev --create-only",
    "db:studio": "prisma studio",
    "d1:push:dev": "wrangler d1 execute potato-charts-dev --remote --file=./prisma/migrations/$(ls -t prisma/migrations | head -1)/migration.sql",
    "d1:push:prod": "wrangler d1 execute potato-charts --remote --file=./prisma/migrations/$(ls -t prisma/migrations | head -1)/migration.sql",
    "d1:push:all:dev": "for file in prisma/migrations/*/migration.sql; do wrangler d1 execute potato-charts-dev --remote --file=\"$file\"; done",
    "d1:push:all:prod": "for file in prisma/migrations/*/migration.sql; do wrangler d1 execute potato-charts --remote --file=\"$file\"; done"
  },
  "dependencies": {
    "@prisma/adapter-d1": "^6.1.0",
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "@workspace/typescript-config": "workspace:*",
    "prisma": "^6.1.0",
    "typescript": "^5.9.2"
  }
}
